# 南京橙龙科技有限公司官网

> 一个基于 Next.js 14 构建的现代化全栈企业官网，包含用户认证系统和数据库集成

[![Next.js](https://img.shields.io/badge/Next.js-14-black)](https://nextjs.org/)
[![TypeScript](https://img.shields.io/badge/TypeScript-5.9-blue)](https://www.typescriptlang.org/)
[![MongoDB](https://img.shields.io/badge/MongoDB-Atlas-green)](https://www.mongodb.com/cloud/atlas)
[![Vercel](https://img.shields.io/badge/Deploy-Vercel-black)](https://vercel.com)

---

## 📋 目录

- [项目简介](#项目简介)
- [技术栈详解](#技术栈详解)
- [系统架构设计](#系统架构设计)
- [功能特性](#功能特性)
- [项目结构](#项目结构)
- [开发环境搭建](#开发环境搭建)
- [数据库配置](#数据库配置)
- [API 接口文档](#api-接口文档)
- [前端组件说明](#前端组件说明)
- [认证流程详解](#认证流程详解)
- [开发工作流](#开发工作流)
- [部署指南](#部署指南)
- [常见问题](#常见问题)
- [贡献指南](#贡献指南)

---

## 📖 项目简介

南京橙龙科技有限公司官方网站是一个现代化的全栈 Web 应用，采用前后端分离架构，提供：

- 🏢 **企业展示**：公司介绍、业务展示、团队介绍
- 🔐 **用户认证**：完整的注册、登录、登出功能
- 🔒 **权限控制**：受保护的资料下载页面
- 💾 **数据持久化**：用户数据存储在 MongoDB 云数据库
- 🌐 **跨设备访问**：用户可在任何设备上登录访问

### 项目特点

- ✅ **全栈应用**：前端 + 后端 API + 数据库
- ✅ **类型安全**：使用 TypeScript 确保代码质量
- ✅ **现代化 UI**：响应式设计，支持移动端和桌面端
- ✅ **安全可靠**：密码加密、JWT 认证、环境变量保护
- ✅ **易于部署**：支持 Vercel 一键部署

---

## 🛠 技术栈详解

### 前端技术栈

#### 1. **Next.js 14** - React 全栈框架
- **作用**：提供服务器端渲染（SSR）、静态生成（SSG）、API 路由等功能
- **为什么选择**：
  - 内置 API 路由，无需单独的后端服务器
  - 自动代码分割和优化
  - 优秀的开发体验和性能
- **版本**：14.0.0（使用 App Router）

#### 2. **TypeScript** - 类型安全的 JavaScript
- **作用**：为 JavaScript 添加静态类型检查
- **为什么选择**：
  - 减少运行时错误
  - 提供更好的 IDE 支持
  - 提高代码可维护性
- **版本**：5.9.3

#### 3. **React 18** - UI 框架
- **作用**：构建用户界面的核心库
- **特性**：
  - 组件化开发
  - 虚拟 DOM
  - Hooks API
- **版本**：18.2.0

#### 4. **Tailwind CSS** - 实用优先的 CSS 框架
- **作用**：快速构建现代化 UI
- **为什么选择**：
  - 无需编写自定义 CSS
  - 响应式设计简单
  - 高度可定制
- **版本**：3.3.0

#### 5. **Framer Motion** - 动画库
- **作用**：为 React 组件添加流畅的动画效果
- **特性**：
  - 声明式动画 API
  - 手势支持
  - 布局动画
- **版本**：10.18.0

#### 6. **Heroicons** - SVG 图标库
- **作用**：提供精美的图标
- **版本**：2.0.18

### 后端技术栈

#### 1. **Next.js API Routes** - 后端 API
- **作用**：在 Next.js 中创建 API 端点
- **位置**：`app/api/` 目录
- **特性**：
  - 无需单独的后端服务器
  - 自动处理 CORS
  - 支持中间件

#### 2. **MongoDB** - NoSQL 数据库
- **作用**：存储用户数据
- **为什么选择**：
  - 灵活的文档模型
  - 易于扩展
  - 免费云服务（MongoDB Atlas）
- **版本**：使用 MongoDB Atlas（云数据库）

#### 3. **Mongoose** - MongoDB 对象建模工具
- **作用**：在 Node.js 中操作 MongoDB
- **功能**：
  - 定义数据模型（Schema）
  - 数据验证
  - 中间件支持（如密码加密）
- **版本**：8.20.0

#### 4. **JWT (JSON Web Token)** - 身份认证
- **作用**：实现无状态的用户认证
- **为什么选择**：
  - 无需服务器端存储 session
  - 跨域友好
  - 易于扩展
- **库**：jsonwebtoken 9.0.2

#### 5. **bcryptjs** - 密码加密
- **作用**：加密存储用户密码
- **为什么选择**：
  - 行业标准加密算法
  - 单向加密（无法解密）
  - 自动加盐（salt）
- **版本**：3.0.3

### 开发工具

- **ESLint**：代码质量检查
- **PostCSS**：CSS 处理工具
- **Autoprefixer**：自动添加 CSS 前缀

---

## 🏗 系统架构设计

### 整体架构图

```
┌─────────────────────────────────────────────────────────┐
│                      用户浏览器                          │
│  (React 前端页面 + 客户端 JavaScript)                   │
└──────────────────┬──────────────────────────────────────┘
                   │ HTTP/HTTPS
                   │
┌──────────────────▼──────────────────────────────────────┐
│              Next.js 应用服务器                         │
│  ┌──────────────────────────────────────────────────┐  │
│  │  前端页面 (SSR/SSG)                              │  │
│  │  - 首页、登录页、注册页、资料下载页等            │  │
│  └──────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────┐  │
│  │  API 路由 (后端)                                 │  │
│  │  - /api/auth/register (注册)                     │  │
│  │  - /api/auth/login (登录)                        │  │
│  │  - /api/auth/me (获取用户信息)                   │  │
│  └──────────────────────────────────────────────────┘  │
└──────────────────┬──────────────────────────────────────┘
                   │
                   │ MongoDB 连接
                   │
┌──────────────────▼──────────────────────────────────────┐
│            MongoDB Atlas (云数据库)                      │
│  ┌──────────────────────────────────────────────────┐  │
│  │  users 集合                                      │  │
│  │  - _id, email, password (加密), name, timestamps│  │
│  └──────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

### 数据流设计

#### 1. **用户注册流程**

```
用户填写表单
    ↓
前端验证（邮箱格式、密码长度等）
    ↓
POST /api/auth/register
    ↓
后端验证（检查邮箱是否已存在）
    ↓
密码加密（bcrypt）
    ↓
保存到 MongoDB
    ↓
生成 JWT Token
    ↓
返回用户信息和 Token
    ↓
前端保存 Token 到 localStorage
    ↓
更新 AuthContext 状态
    ↓
跳转到首页
```

#### 2. **用户登录流程**

```
用户输入邮箱和密码
    ↓
POST /api/auth/login
    ↓
从 MongoDB 查找用户
    ↓
验证密码（bcrypt.compare）
    ↓
生成 JWT Token
    ↓
返回用户信息和 Token
    ↓
前端保存 Token
    ↓
更新 AuthContext
    ↓
跳转到首页或目标页面
```

#### 3. **访问受保护页面流程**

```
用户访问 /downloads
    ↓
检查 AuthContext 中的 user 状态
    ↓
如果没有 user，检查 localStorage 中的 token
    ↓
如果有 token，调用 GET /api/auth/me
    ↓
后端验证 token（JWT.verify）
    ↓
从 MongoDB 获取用户信息
    ↓
返回用户信息
    ↓
更新 AuthContext
    ↓
显示页面内容
    ↓
如果没有 token 或验证失败
    ↓
重定向到 /login
```

### API 设计原则

1. **RESTful 风格**：使用标准的 HTTP 方法（GET、POST）
2. **统一响应格式**：成功返回数据，失败返回错误信息
3. **错误处理**：使用 HTTP 状态码（200、400、401、500）
4. **安全性**：密码加密、JWT 认证、环境变量保护

---

## ✨ 功能特性

### 公开页面（无需登录）

1. **首页** (`/`)
   - 公司介绍
   - 业务展示
   - 团队介绍
   - 统计数据
   - 联系方式

2. **业务页面**
   - `/business/llm` - 大模型开发教学
   - `/business/embedded` - 嵌入式开发教学
   - `/business/publish` - 科技出版服务

3. **其他页面**
   - `/campus-recruitment` - 校园招聘
   - `/team-building` - 公司团建
   - `/partners` - 合作伙伴
   - `/financial-reports` - 公司财报

### 认证相关页面

1. **注册页面** (`/register`)
   - 用户注册表单
   - 表单验证
   - 密码确认
   - 错误提示

2. **登录页面** (`/login`)
   - 邮箱和密码登录
   - 记住登录状态（Token）
   - 错误提示

### 受保护页面（需要登录）

1. **资料下载页面** (`/downloads`)
   - 需要登录才能访问
   - 未登录自动跳转到登录页
   - 显示各种资料下载

---

## 📁 项目结构

```
orange-dragon-web-v2-main/
│
├── app/                          # Next.js App Router 目录
│   ├── layout.tsx               # 根布局（包含 AuthProvider）
│   ├── page.tsx                 # 首页
│   ├── globals.css              # 全局样式
│   │
│   ├── api/                     # API 路由（后端）
│   │   └── auth/                # 认证相关 API
│   │       ├── register/
│   │       │   └── route.ts     # POST /api/auth/register
│   │       ├── login/
│   │       │   └── route.ts     # POST /api/auth/login
│   │       └── me/
│   │           └── route.ts     # GET /api/auth/me
│   │
│   ├── login/                   # 登录页面
│   │   └── page.tsx
│   ├── register/                # 注册页面
│   │   └── page.tsx
│   ├── downloads/               # 资料下载页面（受保护）
│   │   └── page.tsx
│   │
│   └── business/                # 业务相关页面
│       ├── page.tsx
│       ├── llm/
│       ├── embedded/
│       └── publish/
│
├── components/                   # React 组件
│   ├── Navigation.tsx           # 导航栏（包含登录状态）
│   ├── ContactSection.tsx       # 联系部分
│   └── BackToHomeButton.tsx     # 返回首页按钮
│
├── contexts/                    # React Context（状态管理）
│   └── AuthContext.tsx          # 认证上下文
│       - user: 当前用户信息
│       - login: 登录函数
│       - register: 注册函数
│       - logout: 登出函数
│       - isLoading: 加载状态
│
├── lib/                         # 工具库
│   ├── mongodb.ts               # MongoDB 连接管理
│   │   - connectDB(): 连接数据库
│   │   - 连接缓存机制
│   └── jwt.ts                   # JWT 工具函数
│       - generateToken(): 生成 token
│       - verifyToken(): 验证 token
│
├── models/                      # 数据模型（Mongoose Schema）
│   └── User.ts                  # 用户模型
│       - email: 邮箱（唯一、必填）
│       - password: 密码（加密存储）
│       - name: 姓名（必填）
│       - timestamps: 创建和更新时间
│       - comparePassword(): 密码比较方法
│
├── public/                      # 静态资源
│   ├── business-images/         # 业务图片
│   └── university-logos/        # 大学 Logo
│
├── .env.local                   # 环境变量（不提交到 Git）
│   - MONGODB_URI: 数据库连接字符串
│   - JWT_SECRET: JWT 密钥
│
├── package.json                 # 项目配置和依赖
├── tsconfig.json                # TypeScript 配置
├── tailwind.config.ts           # Tailwind CSS 配置
├── next.config.mjs              # Next.js 配置
│
└── 文档/
    ├── README.md                # 本文件
    ├── ENV_SETUP.md             # 环境变量配置指南
    ├── MONGODB_ATLAS_SETUP.md   # MongoDB Atlas 设置指南
    ├── VERCEL_DEPLOYMENT.md     # Vercel 部署指南
    └── VIEW_USER_DATA.md        # 查看用户数据指南
```

---

## 🚀 开发环境搭建

### 前置要求

- **Node.js**: >= 18.0.0
- **npm**: >= 9.0.0（或 yarn）
- **Git**: 用于版本控制
- **代码编辑器**: VS Code（推荐）

### 步骤 1：克隆项目

```bash
git clone https://github.com/ZHChen2000/orange-dragon-web-v2-main.git
cd orange-dragon-web-v2-main
```

### 步骤 2：安装依赖

```bash
npm install
# 或
yarn install
```

这会安装所有依赖包，包括：
- Next.js、React、TypeScript
- MongoDB 相关包（mongoose）
- 认证相关包（jsonwebtoken、bcryptjs）
- UI 相关包（tailwindcss、framer-motion）

### 步骤 3：配置环境变量

创建 `.env.local` 文件：

```bash
touch .env.local
```

编辑 `.env.local`，添加以下内容：

```env
# MongoDB 连接字符串
MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/orange-dragon-web?retryWrites=true&w=majority

# JWT 密钥（用于生成和验证 token）
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
```

**重要**：
- 替换 `username` 和 `password` 为你的 MongoDB Atlas 凭据
- 替换 `cluster0.xxxxx.mongodb.net` 为你的集群地址
- 生成 JWT_SECRET：`openssl rand -base64 32`

详细配置请参考：[ENV_SETUP.md](./ENV_SETUP.md)

### 步骤 4：启动开发服务器

```bash
npm run dev
# 或
yarn dev
```

访问：http://localhost:3000

### 步骤 5：验证安装

1. 打开浏览器访问 http://localhost:3000
2. 应该能看到首页
3. 点击"注册"按钮，尝试注册一个测试账号
4. 如果注册成功，说明环境配置正确

---

## 🗄 数据库配置

### MongoDB Atlas 设置（推荐）

MongoDB Atlas 是 MongoDB 的云数据库服务，提供免费的 512MB 存储空间。

#### 快速设置步骤

1. **注册账号**
   - 访问：https://www.mongodb.com/cloud/atlas/register
   - 使用邮箱注册（或 Google 账号）

2. **创建免费集群**
   - 选择 FREE (M0) 套餐
   - 选择地区（推荐：新加坡或日本）
   - 等待集群创建（3-5 分钟）

3. **创建数据库用户**
   - Database Access → Add New Database User
   - 设置用户名和密码（保存好密码！）
   - 权限：Atlas admin

4. **配置网络访问**
   - Network Access → Add IP Address
   - 开发环境：选择 "Allow Access from Anywhere" (0.0.0.0/0)
   - 生产环境：只添加特定 IP

5. **获取连接字符串**
   - Database → Connect → Connect your application
   - 复制连接字符串
   - 替换 `<password>` 为你的密码
   - 添加数据库名称：`/orange-dragon-web`

详细步骤请参考：[MONGODB_ATLAS_SETUP.md](./MONGODB_ATLAS_SETUP.md)

### 数据库结构

#### users 集合

```javascript
{
  _id: ObjectId("..."),           // MongoDB 自动生成的唯一 ID
  email: "user@example.com",      // 用户邮箱（唯一索引）
  password: "$2b$10$...",         // bcrypt 加密后的密码
  name: "用户名",                  // 用户姓名
  createdAt: ISODate("..."),      // 创建时间
  updatedAt: ISODate("...")       // 更新时间
}
```

### 查看数据库数据

1. **MongoDB Atlas 控制台**
   - 登录 https://cloud.mongodb.com/
   - Database → Browse Collections
   - 选择 `orange-dragon-web` → `users`

2. **MongoDB Compass**（桌面应用）
   - 下载：https://www.mongodb.com/try/download/compass
   - 使用连接字符串连接

详细说明请参考：[VIEW_USER_DATA.md](./VIEW_USER_DATA.md)

---

## 📡 API 接口文档

### 基础信息

- **Base URL**: `http://localhost:3000/api`（开发环境）
- **Content-Type**: `application/json`
- **认证方式**: Bearer Token（JWT）

### 1. 用户注册

**端点**: `POST /api/auth/register`

**请求体**:
```json
{
  "email": "user@example.com",
  "password": "password123",
  "name": "用户名"
}
```

**响应** (201 Created):
```json
{
  "message": "注册成功",
  "user": {
    "id": "507f1f77bcf86cd799439011",
    "email": "user@example.com",
    "name": "用户名"
  },
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
}
```

**错误响应** (400 Bad Request):
```json
{
  "error": "该邮箱已被注册"
}
```

### 2. 用户登录

**端点**: `POST /api/auth/login`

**请求体**:
```json
{
  "email": "user@example.com",
  "password": "password123"
}
```

**响应** (200 OK):
```json
{
  "message": "登录成功",
  "user": {
    "id": "507f1f77bcf86cd799439011",
    "email": "user@example.com",
    "name": "用户名"
  },
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
}
```

**错误响应** (401 Unauthorized):
```json
{
  "error": "邮箱或密码错误"
}
```

### 3. 获取当前用户信息

**端点**: `GET /api/auth/me`

**请求头**:
```
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

**响应** (200 OK):
```json
{
  "user": {
    "id": "507f1f77bcf86cd799439011",
    "email": "user@example.com",
    "name": "用户名"
  }
}
```

**错误响应** (401 Unauthorized):
```json
{
  "error": "未授权访问"
}
```

---

## 🧩 前端组件说明

### 1. AuthContext（认证上下文）

**位置**: `contexts/AuthContext.tsx`

**作用**: 全局管理用户认证状态

**API**:
```typescript
const { user, login, register, logout, isLoading } = useAuth();

// user: 当前用户信息（null 表示未登录）
// login(email, password): 登录函数，返回 { success, error? }
// register(email, password, name): 注册函数，返回 { success, error? }
// logout(): 登出函数
// isLoading: 是否正在加载用户信息
```

**使用示例**:
```typescript
import { useAuth } from '@/contexts/AuthContext';

function MyComponent() {
  const { user, logout } = useAuth();
  
  if (user) {
    return <div>欢迎，{user.name}</div>;
  }
  
  return <div>请登录</div>;
}
```

### 2. Navigation（导航栏）

**位置**: `components/Navigation.tsx`

**功能**:
- 显示网站 Logo 和导航链接
- 根据登录状态显示"登录/注册"或"用户名/登出"
- 响应式设计

### 3. 页面组件

所有页面都在 `app/` 目录下，使用 Next.js App Router：

- **客户端组件**: 使用 `"use client"` 指令
- **服务端组件**: 默认（无需指令）

---

## 🔐 认证流程详解

### JWT Token 机制

#### 1. Token 生成

当用户注册或登录成功后，服务器会生成 JWT Token：

```typescript
// lib/jwt.ts
const token = jwt.sign(
  { userId: user._id, email: user.email },
  JWT_SECRET,
  { expiresIn: '7d' }  // 7 天后过期
);
```

#### 2. Token 存储

前端将 Token 存储在 `localStorage`：

```typescript
localStorage.setItem('token', token);
```

#### 3. Token 验证

每次请求受保护的 API 时，前端会在请求头中携带 Token：

```typescript
fetch('/api/auth/me', {
  headers: {
    'Authorization': `Bearer ${token}`
  }
});
```

服务器验证 Token：

```typescript
const payload = jwt.verify(token, JWT_SECRET);
// 如果验证失败，返回 401
```

#### 4. Token 过期

Token 过期后，用户需要重新登录。

### 密码加密流程

#### 注册时加密

```typescript
// models/User.ts
UserSchema.pre('save', async function (next) {
  if (!this.isModified('password')) return next();
  
  const salt = await bcrypt.genSalt(10);  // 生成盐值
  this.password = await bcrypt.hash(this.password, salt);  // 加密密码
  next();
});
```

#### 登录时验证

```typescript
const user = await User.findOne({ email });
const isValid = await user.comparePassword(password);
// comparePassword 内部使用 bcrypt.compare
```

### 安全措施

1. **密码加密**: 使用 bcrypt，单向加密，无法解密
2. **Token 过期**: 7 天自动过期
3. **环境变量**: 敏感信息存储在环境变量中
4. **HTTPS**: 生产环境使用 HTTPS 传输

---

## 💻 开发工作流

### 1. 本地开发

```bash
# 启动开发服务器
npm run dev

# 代码会自动热重载
# 访问 http://localhost:3000
```

### 2. 代码检查

```bash
# 运行 ESLint
npm run lint

# 修复自动修复的问题
npm run lint -- --fix
```

### 3. 构建测试

```bash
# 构建生产版本
npm run build

# 启动生产服务器（测试）
npm start
```

### 4. 提交代码

```bash
# 添加更改
git add .

# 提交（使用有意义的提交信息）
git commit -m "feat: 添加新功能"

# 推送到 GitHub
git push origin main
```

### 5. 部署

推送到 GitHub 后，Vercel 会自动部署（如果已配置）。

---

## 🚢 部署指南

### Vercel 部署（推荐）

Vercel 是 Next.js 的官方部署平台，提供免费套餐。

#### 步骤 1：准备代码

确保代码已推送到 GitHub：

```bash
git add .
git commit -m "准备部署"
git push origin main
```

#### 步骤 2：连接 Vercel

1. 访问 https://vercel.com
2. 使用 GitHub 账号登录
3. 点击 "New Project"
4. 导入你的 GitHub 仓库

#### 步骤 3：配置环境变量

在 Vercel 项目设置中添加：

1. **MONGODB_URI**
   ```
   mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/orange-dragon-web?retryWrites=true&w=majority
   ```

2. **JWT_SECRET**
   ```
   你的 JWT 密钥
   ```

**重要**：选择所有环境（Production、Preview、Development）

#### 步骤 4：部署

1. 点击 "Deploy"
2. 等待部署完成（1-3 分钟）
3. 访问部署的 URL

详细步骤请参考：[VERCEL_DEPLOYMENT.md](./VERCEL_DEPLOYMENT.md)

### 其他部署选项

- **Docker**: 可以容器化部署
- **自建服务器**: 需要 Node.js 环境和 MongoDB
- **其他平台**: Railway、Render、Netlify 等

---

## ❓ 常见问题

### Q1: 如何重置数据库？

在 MongoDB Atlas 控制台：
1. Database → Browse Collections
2. 选择 `users` 集合
3. 点击 "Delete" 删除集合
4. 重新注册用户时会自动创建

### Q2: 忘记密码怎么办？

当前版本不支持密码重置功能。可以：
1. 在 MongoDB Atlas 中删除用户
2. 重新注册

或者添加密码重置功能（需要实现邮箱验证）。

### Q3: 如何修改用户信息？

当前版本不支持修改用户信息。可以：
1. 添加用户信息编辑 API
2. 添加前端编辑页面

### Q4: 部署后无法连接数据库？

检查：
1. MongoDB Atlas 网络访问设置（允许所有 IP）
2. Vercel 环境变量是否正确配置
3. 连接字符串格式是否正确

### Q5: Token 过期后怎么办？

用户需要重新登录。Token 过期时间为 7 天。

### Q6: 如何添加新的 API 端点？

1. 在 `app/api/` 下创建新目录
2. 创建 `route.ts` 文件
3. 导出 HTTP 方法函数（GET、POST 等）

示例：
```typescript
// app/api/example/route.ts
import { NextRequest, NextResponse } from 'next/server';

export async function GET(request: NextRequest) {
  return NextResponse.json({ message: 'Hello' });
}
```

---

## 🤝 贡献指南

### 开发规范

1. **代码风格**: 使用 ESLint 和 Prettier
2. **提交信息**: 使用约定式提交（Conventional Commits）
   - `feat:` 新功能
   - `fix:` 修复 bug
   - `docs:` 文档更新
   - `style:` 代码格式
   - `refactor:` 重构

3. **分支管理**: 
   - `main`: 主分支（生产环境）
   - `develop`: 开发分支
   - `feature/*`: 功能分支

### 添加新功能

1. 创建功能分支：`git checkout -b feature/new-feature`
2. 开发功能
3. 测试功能
4. 提交代码：`git commit -m "feat: 添加新功能"`
5. 推送分支：`git push origin feature/new-feature`
6. 创建 Pull Request

---

## 📚 学习资源

### Next.js
- [官方文档](https://nextjs.org/docs)
- [App Router 文档](https://nextjs.org/docs/app)

### React
- [官方文档](https://react.dev)
- [React Hooks](https://react.dev/reference/react)

### TypeScript
- [官方文档](https://www.typescriptlang.org/docs)
- [TypeScript 手册](https://www.typescriptlang.org/docs/handbook/intro.html)

### MongoDB
- [MongoDB 文档](https://docs.mongodb.com)
- [Mongoose 文档](https://mongoosejs.com/docs)

### Tailwind CSS
- [官方文档](https://tailwindcss.com/docs)

---

## 📞 联系方式

- 📧 **邮箱**: zhchen2000@foxmail.com
- 📞 **电话**: +86 13201301067
- 📍 **地址**: 江苏省南京市秦淮区光华路街道光华路127号3层304F室
- 🌐 **网站**: https://www.orandrag.com/

---

## 📄 许可证

Copyright © 2024 南京橙龙科技有限公司 保留所有权利

---

## 🙏 致谢

感谢以下开源项目：

- [Next.js](https://nextjs.org/)
- [React](https://react.dev/)
- [MongoDB](https://www.mongodb.com/)
- [Tailwind CSS](https://tailwindcss.com/)
- [Vercel](https://vercel.com/)

---

**最后更新**: 2025年11月

**项目版本**: 2.0.0
